apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: simple-app-dashboard

namePrefix: dashboard-

resources:
  - rbac.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: dashboard
    newTag: "v1"
