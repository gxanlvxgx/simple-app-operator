apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: simple-app-dashboard
spec:
  selector:
    app: dashboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
