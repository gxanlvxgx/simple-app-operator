apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: simpleapps.apps.myapp.io
spec:
  group: apps.myapp.io
  names:
    kind: SimpleApp
    listKind: SimpleAppList
    plural: simpleapps
    singular: simpleapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .spec.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: SimpleApp is the Schema for the simpleapps API
        properties:
          apiVersion:
            description: APIVersion defines the versioned schema of this representation of an object.
            type: string
          kind:
            description: Kind is a string value representing the REST resource this object represents.
            type: string
          metadata:
            type: object
          spec:
            description: SimpleAppSpec defines the desired state of SimpleApp
            properties:
              containerPort:
                description: ContainerPort is the port the application listens on inside the container
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              image:
                description: Image is the Docker image to run
                type: string
              replicas:
                default: 1
                description: Replicas defines how many instances of the application to run
                format: int32
                minimum: 1
                type: integer
              servicePort:
                default: 80
                description: ServicePort is the port exposed by the Kubernetes Service
                format: int32
                type: integer
            required:
            - containerPort
            - image
            type: object
          status:
            description: SimpleAppStatus defines the observed state of SimpleApp
            properties:
              readyReplicas:
                description: ReadyReplicas tells us how many pods are actually running
                format: int32
                type: integer
              serviceStatus:
                description: ServiceStatus reports the general health
                type: string
            required:
            - readyReplicas
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
